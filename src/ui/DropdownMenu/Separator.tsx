import type { VoidComponent } from 'solid-js';

import { Separator } from '~/ui/Separator';

export const MenuSeparator: VoidComponent = () => {
    return <Separator class='my-2 border-neutral-700' />;
};
